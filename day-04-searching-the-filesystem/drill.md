# Day 04 Drill

# task: find the files with .conf inside the /etc directory
command: find /etc -name "*.conf"

# task: find every files larger than 1M
command: find ~ -type f -size +1M

# task: display how much space a directory uses
command: du -sh /home/aleem/stella

# task: display the free space on the system in human-readable format
command: df -h ~
